const initialState = {
    smurf: {
        name: '',
        age: '',
        height: '',
        id: null
    },
    isFetching: false
}

export const smurfReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}